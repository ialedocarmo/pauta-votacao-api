services:
  postgres:
    image: postgres:17
    container_name: pauta-votacao-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: pauta_votacao
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      TZ: America/Sao_Paulo
    ports:
      - "5432:5432"
    volumes:
      - pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d pauta_votacao"]
      interval: 10s
      timeout: 5s
      retries: 10

volumes:
  pg_data:
